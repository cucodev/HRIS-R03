@model BusinessEntities.CrudEntities.UserCredModel

@if (Model == null)
{
    Response.Redirect("~/Default/LogOn");
}
else
{
    <script type="text/javascript">
        //Model Controller
        try {
            var cIDV = '@Model.IDV';
            var cName = '@Model.Name';
            var cImage = '@Model.Image';
            var cIDVParent = '@Model.parentIDV';
            var cIDVMail = '@Model.IDVMail';
            var cIDVParentMail = '@Model.parentIDVMail';
            var cRole = '@Model.role';
            var ctimeLogin = '@Model.timeLogin';

            console.log("======================================================================")
            console.log("_SessionUser.cshtml");
            console.log("======================================================================")
            console.log("cIDV: " + cIDV);
            console.log("cName: " + cName);
            console.log("cImage: " + cImage);
            console.log("cIDVParent: " + cIDVParent);
            console.log("cIDVMail: " + cIDVMail);
            console.log("cIDVParentMail: " + cIDVParentMail);
            console.log("cRole: " + cRole);
            console.log("cTimeLogin: " + ctimeLogin);
            console.log("======================================================================")

            //$("#cImage").attr("src", '@Url.Content(Model.Image)');

        } catch (err) {
            window.open("/");
        }

        $(document).ready(function () {
            $("#cImage").attr("src", '@Url.Content(Model.Image)');
        });

    </script>
}